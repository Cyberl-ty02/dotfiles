post_pkg_postinst() {
  # 你偏好的排除：不去动第三方 WSL 内核那条（可按需删）
  EXCLUDE_GLOBS="*-locietta-WSL2-xanmod1"
  SKIP_RUNNING=1

  if [[ -x /usr/local/sbin/fix-kmod-buildlinks ]]; then
    EXCLUDE_GLOBS="$EXCLUDE_GLOBS" SKIP_RUNNING="$SKIP_RUNNING" \
      /usr/local/sbin/fix-kmod-buildlinks || true
  fi
}

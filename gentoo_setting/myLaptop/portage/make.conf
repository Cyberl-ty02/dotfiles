# Gentoo laptop make.conf
# Profile: default/linux/amd64/23.0/desktop/plasma
# Target: real laptop, GCC, OpenRC, SonicDE/XLibre, NVIDIA, and Secure Boot.

# The selected non-LLVM profile and gcc-config provide the default GCC toolchain.
# Keep package-specific Clang use (for example CUDA) in package.use instead.
COMMON_FLAGS="-march=native -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

LC_MESSAGES=C.utf8

# Preserve the amd64/23.0 profile linker defaults.
LDFLAGS="-Wl,-O1 -Wl,--as-needed -Wl,-z,pack-relative-relocs"
# Optional mold linker override.
#LDFLAGS="-Wl,-O1 -Wl,--as-needed -Wl,-z,pack-relative-relocs -fuse-ld=mold"

# The Plasma profile supplies the normal desktop, Qt, media, and elogind defaults.
# Keep only machine-specific additions and the explicit OpenRC choice here.
USE="btrfs compress-zstd grub modules-sign openmp secureboot smart thunderbolt verify-sig zstd -systemd"

# Secure Boot / module signing.
# MOK.pem contains private key + certificate. Do not share it.
SECUREBOOT_SIGN_KEY="/etc/kernel/secureboot/MOK.pem"
SECUREBOOT_SIGN_CERT="/etc/kernel/secureboot/MOK.pem"
MODULES_SIGN_KEY="/etc/kernel/secureboot/MOK.pem"
MODULES_SIGN_CERT="/etc/kernel/secureboot/MOK.pem"

MAKEOPTS="-j13 -l13"
PORTAGE_NICENESS=15
EMERGE_DEFAULT_OPTS="--autounmask --autounmask-keep-masks --autounmask-write=n --keep-going --jobs=2 --load-average=13"

FEATURES="candy compress-build-logs parallel-fetch userfetch userpriv usersandbox preserve-libs buildpkg binpkg-multi-instance"

L10N="en-US zh-CN"
LINGUAS="en_US zh_CN"

GRUB_PLATFORMS="efi-64"
INPUT_DEVICES="libinput"
VIDEO_CARDS="nvidia modesetting intel"

QEMU_SOFTMMU_TARGETS="x86_64 aarch64"
QEMU_USER_TARGETS="x86_64 aarch64"
# LLVM remains available to package-specific consumers; it is not the system compiler.
LLVM_TARGETS="X86 NVPTX WebAssembly"

# Uncomment only if you accept broad license behavior.
# ACCEPT_LICENSE="*"

FETCHCOMMAND="/usr/bin/curl --retry 3 --connect-timeout 60 -Lfo \"\${DISTDIR}/\${FILE}\" \"\${URI}\""
RESUMECOMMAND="/usr/bin/curl -C - --retry 3 --connect-timeout 60 -Lfo \"\${DISTDIR}/\${FILE}\" \"\${URI}\""

# Optional mirrors / proxy.
#GENTOO_MIRRORS="https://mirrors.ustc.edu.cn/gentoo/"
#https_proxy="http://127.0.0.1:7890"
#http_proxy="http://127.0.0.1:7890"
#all_proxy="socks5h://127.0.0.1:7890"

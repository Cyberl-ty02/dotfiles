# Introduce

This repo is mainly for backup some of my configs.

## nixos_setting

Declarative NixOS configurations for:

- a development-focused WSL environment;
- the primary physical PC/laptop system with Plasma, optional NVIDIA support,
  optional CUDA tools, Btrfs installation helpers and optional Secure Boot.

## alternative_setting

Alternative operating-system configurations retained as fallback choices:

- `gentoo/`: the former primary Gentoo laptop configuration.

## windows_setting

Some of my Windows configuration files.

## dot(s)

For  other configuration files
